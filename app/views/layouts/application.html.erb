<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
	<%= stylesheet_link_tag "styles" %>
	<%= javascript_include_tag :defaults %>
</head>
<body <%= "onload=\"initLightbox();\"" if controller.action_name == "edit" && controller.controller_name == "document" %>>
	<div id="header">
		<% if flash[:notice] %>
			<div id="flash" class="<%= flash[:type] -%>"><%=h flash[:notice] %></div>
		<% end %>
			<h1><%= link_to 'Drawme', '/' %></h1>
			
	</div>
	<div id="content">
		<% if logged_in? %>
			<div id="small-panel">
				Hello <%= current_user.login %>!
				<ul>
					<li id="control-panel"><%= link_to "Control Panel", { :controller => "website", :action => "panel" }%></li>
					<li id="logout"><%= link_to "Logout", { :controller => "sessions", :action => "logout"}%></li>
				</ul>
			</div>
		<% end %>
		
		<%= yield %>
		
	</div>
	<div id="footer" class="clear">
			<p>Drawme is a PFC by Albert Llop. Current version: Alpha</p>
	</div>
</body>
</html>